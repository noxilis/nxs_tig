### --- PERMISSIONS TIG --- ###
# fondateur
add_ace group.fondateur command.tig allow
add_ace group.fondateur command.stoptig allow

# Superadmin
add_ace group.superadmin command.tig allow
add_ace group.superadmin command.stoptig allow

# Admin
add_ace group.admin command.tig allow
add_ace group.admin command.stoptig allow

# Modérateur
add_ace group.moderator command.tig allow
add_ace group.moderator command.stoptig allow

# Helper
add_ace group.helper command.tig allow
add_ace group.helper command.stoptig allow
